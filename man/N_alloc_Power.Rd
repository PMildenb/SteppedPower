% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/N_alloc_Power.R
\name{N_alloc_Power}
\alias{N_alloc_Power}
\title{Power distribution over cluster-to-sequence allocations}
\usage{
N_alloc_Power(
  N_pool,
  Cl,
  ...,
  n_MC = NULL,
  max_alloc = 50000,
  parallel = FALSE,
  keep = "summary",
  verbose = TRUE
)
}
\arguments{
\item{N_pool}{numeric vector of length \code{sum(Cl)}, number of
individuals per cluster, i.e. the pool of per-cluster \code{N} values \emph{before}
allocation to sequences. The order of this vector defines the cluster
identities (cluster 1, 2, ...); the function averages over all ways of
assigning these clusters to sequences.}

\item{Cl}{integer (vector), number of clusters per sequence group,
as in \code{\link{glsPower}}. Must satisfy \code{sum(Cl) == length(N_pool)}.}

\item{...}{further arguments passed to \code{\link{glsPower}}, e.g. \code{mu0},
\code{mu1}, \code{sigma}, \code{tau}, \code{family}, \code{alpha_0_1_2}, \code{eta}, \code{rho}, \code{gamma},
\code{psi}, \code{AR}, \code{sig.level}, \code{trtDelay}, \code{incomplete}, \code{timeAdjust},
\code{dsntype}, \code{timepoints}, \code{INDIV_LVL}. Arguments \code{N} and \code{power} are
managed by \code{N_alloc_Power} and must NOT be supplied via \code{...}.}

\item{n_MC}{integer, number of Monte Carlo allocations to draw uniformly at
random. If \code{NULL} (the default), all distinct allocations are enumerated
exhaustively, subject to \code{max_alloc}. Use this when the number of distinct
allocations is too large to enumerate.}

\item{max_alloc}{integer, upper bound on the number of \emph{unique}
allocations evaluated in exhaustive mode. If the number of unique
allocations exceeds this, the function stops with an error suggesting
to set \code{n_MC}. The underlying enumeration of index-partitions is
capped internally at \code{1e6}.}

\item{parallel}{logical, should the per-allocation \code{\link{glsPower}}
calls be evaluated in parallel via \code{future.apply::future_mapply}?
If \code{TRUE}, package \code{future.apply} is required and the user must select
a parallel backend beforehand, e.g.
\code{future::plan(future::multisession)}. If \code{FALSE} (the default), a
plain \code{mapply} is used.}

\item{keep}{character, one of \code{"summary"} (the default) or \code{"all"}. The
former returns only the summary statistics, the latter additionally the
per-allocation powers and the corresponding cluster-size assignments.}

\item{verbose}{logical, print progress messages.}
}
\value{
An object of class \code{N_alloc_Power} (a list). The \code{summary} element is a
named numeric vector with \code{n_allocations}, \code{n_unique}, \code{mean}, \code{sd},
\code{min}, \code{Q1}, \code{median}, \code{Q3}, \code{max} and \code{IQR}. The \code{power} element gives the
(unique) power values and \code{weights} their multiplicities in exhaustive
mode (all \code{1} in Monte Carlo mode, i.e. equal weights); these two together
define the power distribution and can be plotted with
\code{\link[=plot.N_alloc_Power]{plot}}. The \code{best} and \code{worst} elements
give the cluster-size assignment (N-vector, ordered by sequence) achieving
the maximum and minimum power. If \code{keep="all"}, \code{N_allocations}
additionally contains the unique cluster-size assignments as rows of a
matrix.
}
\description{
Wrapper around \code{\link{glsPower}} for designs in which the clusters
have \emph{known but unequal} sizes. Because the randomization assigns clusters
to sequences, the resulting power is a random variable over the set of
possible allocations.

\code{N_alloc_Power} enumerates (or samples) all distinct ways of assigning the
supplied clusters to the sequences defined by \code{Cl}, computes the power for
each allocation via \code{\link{glsPower}}, and summarises the resulting
distribution (mean, min, max, quartiles, ...).

Within a sequence, the order of clusters does not affect power (cluster
contributions to the information add up commutatively), so an \emph{allocation}
is a partition of the cluster set into the sequences, not a full
permutation. Allocations that differ only by swapping equally-sized
clusters therefore share the same power; \code{N_alloc_Power} de-duplicates
them and weights accordingly.
}
\details{
Let \eqn{N = (N_1, \dots, N_K)} the vector of cluster sizes and
\eqn{\theta:= \mu_1-\mu_0} the treatment effect under investigation.
For each allocation of the clusters to the sequences, the power of the
Wald test of \eqn{\theta} is computed by \code{\link{glsPower}}.
The function returns summary statistics of the resulting power
distribution over all (equally weighted) allocations.
}
\examples{
## 8 clusters of unequal size, 4 sequences with 2 clusters each.
## Exhaustive enumeration (2520 index-partitions).
N_alloc_Power(N_pool = c(20, 30, 25, 15, 40, 10, 35, 22),
              Cl = c(2, 2, 2, 2),
              mu0 = 0.2, mu1 = 0.35, tau = 0.5, family = "binomial")

## Gaussian outcome, vary cluster sizes
N_alloc_Power(N_pool = c(12, 8, 10, 9, 14),
              Cl = rep(1, 5),
              mu0 = 0, mu1 = 1, sigma = 2, tau = 0.33)

## Larger design: use Monte Carlo when enumeration is infeasible.
\dontrun{
set.seed(123)
future::plan(future::multisession)
N_alloc_Power(N_pool = round(runif(16, 10, 40)),
              Cl = rep(2, 8),
              mu0 = 0, mu1 = 1, sigma = 2, tau = 0.33,
              n_MC = 5000, parallel = TRUE)
}

}
